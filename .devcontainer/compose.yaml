services:
  devcontainer:
    build: ./.devcontainer
    command: sleep infinity
    volumes:
      - type: bind
        source: .
        target: /workspaces/chat-supabase
        consistency: cached
    init: true
